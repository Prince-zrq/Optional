#include<stdio.h>
int main(){
	float n,m;
	scanf("%f",&n);
	if(n<=1600) m=0.00;
	if(n>1600&&n<=2500) m=(n-1600)*0.05;
	if(n>2500&&n<=3500) m=(n-1600)*0.10;
	if(n>3500&&n<=4500) m=(n-1600)*0.15;
	if(n>4500) m=(n-1600)*0.20;
	printf("%.2f",m);
}
